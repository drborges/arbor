{
  "name": "@arborjs/store",
  "version": "0.0.1-alpha.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "files": [
    "./dist"
  ],
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "esbuild": "^0.12.12",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.6",
    "typescript": "^4.2.4"
  },
  "scripts": {
    "test": "jest",
    "dev": "jest --watch",
    "prebuild": "yarn install && rm -rf dist",
    "build": "esbuild src/index.ts --bundle --minify --sourcemap --outdir=dist --platform=node --target=node10.4",
    "postbuild": "tsc"
  }
}
